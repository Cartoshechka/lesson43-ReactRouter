# React Router Learning Project

Это учебный проект на React, демонстрирующий работу с современной маршрутизацией через **React Router (Data Router)**.

## 🚀 Основные возможности

- **Современный подход**: Использование `createBrowserRouter` и `RouterProvider`.
- **Централизованный конфиг**: Все маршруты и метаданные страниц хранятся в одном месте (`src/config/routes.tsx`).
- **Динамическое меню**: Навигация и футер автоматически подстраиваются под конфиг маршрутов.
- **Адаптивный дизайн**: Современный интерфейс с центрированным контентом и плавными анимациями.
- **Обработка 404**: Кастомная страница ошибки для несуществующих маршрутов.

## 🛠 Технологии

- **React 19**
- **React Router 7**
- **TypeScript**
- **Vite**
- **Vanilla CSS** (современные переменные и Flexbox)

## 📁 Структура проекта

- `src/config/routes.tsx` — **сердце проекта**. Здесь вы добавляете или изменяете страницы.
- `src/App.tsx` — основная логика инициализации роутера и Layout приложения.
- `src/components/` — React компоненты (Меню, Футер, Страницы).
- `src/index.css` — глобальные стили и оформление.
- `vercel.json` — конфигурация для корректной работы SPA-маршрутизации при деплое на Vercel.

## 🚀 Деплой (Vercel)

Для того чтобы маршруты React Router корректно работали после деплоя (не выдавали 404 при перезагрузке страницы), в проекте добавлен файл `vercel.json`. Он перенаправляет все запросы на `index.html`, позволяя React Router самому управлять навигацией.

## ➕ Как добавить новую страницу?

1. Создайте компонент страницы в папке `components`.
2. Откройте `src/config/routes.tsx`.
3. Добавьте новый объект в массив `routes`:
   ```tsx
   {
     path: '/new-page',
     element: <NewPage />,
     label: 'Новая страница'
   }
   ```
4. Страница **автоматически** появится в главном меню и станет доступна по указанному пути.

## 📋 Команды

- `yarn dev` — запуск сервера разработки.
- `yarn build` — сборка проекта для продакшена.
- `yarn lint` — проверка кода линтером.
